{
  "name": "fantacalcio-asta",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env server/index.js",
    "start": "node --env-file-if-exists=.env scripts/start.js",
    "server": "node --env-file-if-exists=.env server/index.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "client": "npm --prefix client run dev",
    "import": "node --env-file-if-exists=.env server/import.js",
    "schema": "node server/lib/dumpSchema.js",
    "check": "node --check server/index.js && node --check server/db.js && node --check server/import.js && node --check server/lib/config.js && node --check server/lib/listone.js && node --check server/lib/dumpSchema.js && node --check scripts/start.js"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.1",
    "@fastify/static": "^10.1.3",
    "fastify": "^5",
    "xlsx": "^0.18.5"
  }
}
